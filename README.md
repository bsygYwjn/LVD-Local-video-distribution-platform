# LVD：局域网本地视频中心

LVD 运行在 Windows 10/11 电脑上，把电脑里的本地视频直接分享给同一局域网内的 Android、iPhone、iPad、Windows 和 macOS 浏览器。项目不需要域名，不会把视频上传到互联网。

当前版本采用简单的无账号模式：观看端打开即可选片播放；管理端打开即可管理，但管理页面和管理接口只接受服务器电脑本机访问。

## 一键启动

1. 双击项目根目录中的 `启动LVD.vbs`，启动过程不会出现 CMD 窗口。
2. LVD 会进入 Windows 系统托盘并弹出控制面板，其中包含本机管理网页快捷按钮、观看端地址和二维码。
3. 手机或平板扫描二维码即可打开观看端；之后左键点击托盘图标可随时再次打开控制面板。
4. 控制面板和托盘右键菜单都能快速关闭或启动共享服务。关闭共享服务后 8096 端口停止监听，但托盘控制器继续运行，点击“启动共享服务”即可恢复。
5. 在管理端“总览”点击“选择并添加文件夹”，从 Windows 文件夹窗口中选择视频目录，然后点击“重新扫描”。仍然可以手动输入类似 `D:\Videos` 的完整路径。扫描时会从每部视频抽取一帧作为观看端缩略图，图片写入 `data/cache/thumbnails`，不会修改原视频。
6. 如需登录 Windows 后自动运行，在管理端“运行设置”点击“开启开机自启”。

项目已经自带 Node.js、FFmpeg 和 ffprobe，普通使用不需要另行安装编程工具。

## 访问规则

### 观看端

- 局域网设备打开 `http://服务器局域网IP:8096` 后直接进入媒体库。
- 没有账号、密码、登录会话、设备申请或管理员批准步骤。
- 视频、字幕和字体读取接口对同一局域网内能访问该端口的设备开放。

### 管理端

- 管理地址固定为 `http://127.0.0.1:8096/admin`，只在服务器电脑上使用。
- 服务端根据网络连接来源判断是否为本机，只接受 `127.0.0.1`、`::1` 和 Windows 的 IPv4 回环地址。
- 局域网设备即使手工输入 `/admin` 或直接调用添加目录、管理端扫描、重封装等管理接口，也会收到 `403` 拒绝响应。观看端仅开放扫描已有媒体目录所需的专用接口。
- 管理端没有密码，打开本机地址即可管理媒体库和运行设置。

升级到本版本后，旧状态文件中的账号、密码哈希、设备和登录会话字段会在服务启动时自动移除；媒体目录、视频记录、处理任务和设置继续保留。

因为观看端完全不鉴权，请只在可信任的家庭或个人局域网中使用，不要把 8096 端口映射或转发到公网。Windows 防火墙首次询问时建议只允许“专用网络”。

## 自动扫描与刷新

- 自动扫描开关集中在本机管理端的“视频目录”区域，默认开启。服务端每 30 秒统一扫描一次已添加的媒体目录；新增、删除或改名的视频与字幕会自动反映到观看页。
- “立即刷新”会马上执行一次扫描并更新文件列表，适合刚复制完视频时使用。
- 自动扫描开关是整个 LVD 服务共用的设置，并会保存到 `data/state.json`；只有服务器电脑上的管理端可以修改。
- 多台设备不会各自重复扫描：同一时间只运行一个服务端扫描任务。观看端只能手动刷新已有目录，不能修改自动扫描设置、添加目录、修改作品代号或执行重封装等本机管理操作。

## 网页作品代号与分级菜单

- 管理端会按视频实际所在文件夹列出作品组。每个文件夹只需填写一次作品名和季度。
- LVD 自动从 `[01]`、`E01`、`EP01`、`S01E01`、`第01话` 等常见写法识别集数；无法识别时按文件名自然排序编号。
- 观看端先显示作品文件夹，进入作品后再显示各集，代号格式固定为 `作品名 - S01E01`。
- 代号只存在于 LVD 的状态数据和网页中，不会重命名磁盘上的视频、外挂字幕或字体，也不会影响缩略图和兼容副本。

## 开机自启

- 开关位于本机管理端的“运行设置”。
- 开启后，每次登录 Windows，LVD 会启动共享服务并驻留系统托盘，不会自动弹出管理网页或控制面板。
- 关闭开机自启会移除 LVD 在当前 Windows 用户启动目录中的入口，不会删除项目或媒体库。
- 如果以后移动整个 LVD 文件夹，请在移动前关闭开机自启，移动完成后重新开启，让启动路径更新到新位置。

## 字幕支持

LVD 支持以下文本字幕：

- SRT：由浏览器原生字幕轨显示。SRT 本身只有时间和文字，不包含 ASS 那样的字体、位置和动画信息。
- ASS / SSA：由 JASSUB 和 libass WebAssembly 渲染，可以保留字体样式、位置、描边、淡入淡出、移动和动画标签。
- 内嵌字幕：扫描视频时自动提取内嵌的 SRT、ASS 和 SSA。MKV 等容器中的内嵌字体附件也会提取并提供给 ASS/SSA 渲染器。

外挂字幕可以放在视频同一目录，支持 `.srt`、`.ass`、`.ssa`。同目录字体支持 `.ttf`、`.otf`、`.ttc`、`.woff`、`.woff2`。例如：

```text
电影.mkv
电影.zh-CN.ass
电影.en.srt
MicrosoftYaHei.ttf
```

重新扫描会重新读取外挂字幕和字体，即使视频文件本身没有变化；同名字幕内容更新后，已打开的观看端也会重新加载。字幕与视频完整文件名不同时，单视频文件夹会关联同目录字幕，多集文件夹会按 `[01]`、`E01`、`第01话` 等集数标记安全匹配；集数无法唯一判断时不会自动乱配。

字幕和字体只在播放时叠加，不会烧录进视频，也不会修改原始文件。图片型字幕（例如 PGS）暂不支持。

## 直传、格式、HDR 和重封装

服务端支持 HTTP Range，浏览器只请求正在播放的文件片段，可以拖动进度条，不需要先下载完整视频。媒体库会扫描 MP4、MKV、MOV、M4V、WebM、AVI、TS、M2TS、MTS、MPG、MPEG 和 FLV。

能被 LVD 扫描不等于所有浏览器都能直接解码。实际兼容性取决于设备对容器、视频编码、音频编码、10-bit 和 HDR 的支持。

如果“带字幕的视频无法播放”，请先看文件格式：字幕只是在画面上叠加，常见的真正原因是带字幕片源同时使用了浏览器不易直放的 MKV 容器或 FLAC/Opus 音频。新版播放页会显示实际兼容性提示，并默认先关闭字幕，让视频解码与字幕加载互不干扰。

LVD 会在启动或扫描后自动检查容器与音频组合。发现浏览器不易直放的 MKV、FLAC/Opus 等情况时，会自动加入兼容处理队列，无需手动点击：

- 视频使用 FFmpeg 的 `-c:v copy` 原样复制，不重新编码，画质不变。
- 10-bit 和 HDR 信息随原视频码流保留。
- 不兼容音频可以转换为 AAC 256 kbps；AAC 转换不是数学意义上的无损，但视频码流保持不变。
- 原始视频不会被覆盖，兼容副本写入 `data/cache`。
- 自动任务每次只运行 1 个，避免同时启动大量 FFmpeg 进程；副本所需空间通常接近原视频大小。
- 观看端在后台轮询处理状态；兼容副本完成后，已打开的播放器会自动切换到 MP4 + AAC 地址。
- 如果设备不支持原视频编码，仅重封装仍无法解决；当前版本不自动转码视频。

## 10 路播放能力

服务端最多允许 10 路视频同时传输。原文件直传不会执行十次视频编码，主要消耗硬盘读取和局域网带宽；无损重封装也不会占用 RTX 3060 的 NVENC 编码资源。

在十代 i5、RTX 3060、16 GB 内存、2.5G 有线服务器和千兆无线网络基础上，架构面向 10 路 1080p 10-bit 直传。实际体验仍取决于视频峰值码率、硬盘并发读取、无线 AP、信号质量及客户端硬件解码能力。服务端的 Range 分段传输和 10 路限制已经验证，但尚未用十台真实设备完成长期压力测试。

## 代码目录，用生活化方式理解

```text
src/main.tsx          网页的“大脑”：管理页、观看页、播放器和字幕选择
src/styles.css        网页的“装修”：颜色、尺寸与手机/平板响应式布局
server/index.mjs      共享服务：本机管理限制、扫描、字幕提取和视频直传
tray.ps1              系统托盘控制器：管理页快捷方式、二维码和共享服务启停
tray-qr.mjs           使用离线二维码组件生成托盘窗口中的二维码图片
vendor/qrcode.cjs     离线二维码生成器，运行时不访问互联网
启动LVD.vbs           Windows 双击入口；隐藏启动托盘，不出现 CMD 窗口
tools/ffmpeg/bin      读取媒体信息、提取字幕字体、重封装和转换 AAC
runtime/node.exe      运行 LVD 服务所需的内置 Node.js
data/state.json       媒体目录、视频记录、任务和运行设置
data/cache            内嵌字幕、字体和兼容副本缓存
dist                  构建完成的网页文件
```

浏览器页面只是操作面板；“管理端只能在本机操作”的判断写在 `server/index.mjs` 中。因此，局域网设备无法通过绕过网页按钮来执行管理操作。

## 页面打不开时

1. 在服务器电脑打开 `http://127.0.0.1:8096/api/health`。能看到包含 `"ok":true` 的内容，说明服务正在运行。
2. 手机不能使用 `127.0.0.1`，因为这个地址在手机上表示手机自己；必须使用服务器电脑的局域网 IP。
3. 确认两台设备位于同一局域网，并检查 Windows 防火墙是否允许 LVD 通过专用网络。
4. 局域网设备不能打开 `/admin` 是正常行为；管理端只能在服务器电脑本机打开。

## 开发命令（普通使用可以忽略）

```powershell
npm install
npm run build
npm run server
```

网页源码修改后需要重新构建。服务监听 `0.0.0.0:8096`，观看端通过服务器的局域网 IP 加端口访问。
